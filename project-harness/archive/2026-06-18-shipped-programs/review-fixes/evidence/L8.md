# Lens L8 — Regression Verification Evidence

**Tree:** branch `munich-demo`, HEAD `327180a`
**Date:** 2026-06-13
**Rubric:** Verify (a) fix code present in tree AND (b) guard test present + meaningful (not deleted / skipped / asserted-not-measured)
**Prior review:** `project-harness/archive/review-2026-06-03/FINAL_REVIEW_FINDINGS.md` §3 triage table

---

## 1. Re-Verification Table

All "Fix now" SEVs from the Final Review triage table are checked.

| SEV-id | Fix code present? | Guard test present + meaningful? | Verdict | Key file:line |
|--------|------------------|----------------------------------|---------|---------------|
| **L9-01** game_schema.yaml restored + CI boot gate | YES — file at `src/npc_engine/game_schema.yaml`; lifespan calls `get_game_schema()` (main.py:89-90) | YES — `tests/unit/test_schema_loader.py` has 6 tests exercising load/raise path; `test_boot_smoke.py` confirms app boots | **HOLDS** | `src/npc_engine/game_schema.yaml`, `src/npc_engine/main.py:90` |
| **L9-02** world_state partial-update 422 | YES — `demo_game/client.py:909-931` uses `patch_node` not `upsert_node`; `world_state_writer.py` writes full MERGE | YES — `tests/unit/test_sev13_world_state_id.py:33-65` asserts `patch_node` called, `upsert_node` raises, `faction_standings` not clobbered | **HOLDS** | `demo_game/client.py:931`, `tests/unit/test_sev13_world_state_id.py:33` |
| **L9-05** build-SHA in /health | YES — `config.py:72 BUILD_SHA: str = "dev"`; `system.py:47` returns `get_settings().BUILD_SHA` | PARTIAL — `test_boot_smoke.py` asserts `/health` returns 200 but does NOT assert `version` field contains a SHA or that `BUILD_SHA` is injected via env at build time; no test verifies a non-"dev" value is ever set | **PARTIAL** | `src/npc_engine/api/routes/system.py:47`, `tests/unit/test_boot_smoke.py:55` |
| **L4-01** 8 ISSUE-056 unit fixes | YES — ISSUE-056 marked [FIXED]; 2141 tests pass, 0 fail per L9 evidence | YES — all 8 root-cause tests are now passing per ISSUES.md fix note | **HOLDS** | `project-harness/ISSUES.md` [FIXED] ISSUE-056 |
| **L4-02** SEV-22 seed-log test asserted-not-measured | PARTIAL — `_secret_rng_seed` returns int; `LOGGER.debug()` IS emitted in `gossip_handler.py:391-395` | **REGRESSED** — `test_sev22_rng_determinism.py:75-83` `test_secret_propagation_logs_seed` imports `_secret_rng_seed`, calls it, and only asserts `isinstance(seed, int)` and `seed >= 0`. It never calls `caplog.at_level()`, never asserts `caplog.records`, and the `caplog` fixture is unused. The log emission is not actually measured by this test. | **PARTIAL** | `tests/unit/test_sev22_rng_determinism.py:75-83`, `src/npc_engine/engines/gossip/gossip_handler.py:391` |
| **L4-03** `evals/runner.py` excluded from coverage gate | PARTIAL — `test_eval_runner_guards.py` tests runner via `sys.path.insert`; Makefile line 50 `--cov=evals` covers it in `test-cov-full-report` | PARTIAL — `test-cov-full-report` (line 50) is explicitly **non-gating** in CI (`coverage-gate` runs `make test-cov` at line 44 which only covers `npc_engine --cov=matchers --cov=summary`). `evals/runner.py` is outside the gating coverage floor. | **PARTIAL** | `Makefile:44,50`, `.github/workflows/ci.yml: coverage-gate` |
| **L4-04** SEV-18 gossip mock missing `distortion_level` | YES — `test_error_swallowing_sev18.py:221` now includes `"distortion_level": 100` | YES — test asserts `pytest.raises(RuntimeError, match="rumor db down")` meaning `create_rumor` is actually reached and raises | **HOLDS** | `tests/unit/test_error_swallowing_sev18.py:221` |
| **L4-06** Eval tests manual `sys.path` injection | **NOT FIXED** — `test_eval_matchers.py:22-23`, `test_eval_matchers_sev38.py:23-24`, `test_eval_runner_guards.py:15-16`, `test_eval_summary.py:15`, `test_anti_hallucination_runner.py:17` all still use `sys.path.insert(0, ...)` | No guard test exists for this — it is a harness hygiene item, not a behavioral contract | **REGRESSED** | `tests/unit/test_eval_matchers.py:22`, multiple files |
| **L4-07** tone_judge infra failure keeps `guarantee_demonstrated=True` | YES — `evals/runner.py:236-246` treats `score=None` as inconclusive, `case_passed=False`; `evals/matchers.py:215-216` returns `JudgeResult(score=None, error="infra_failure")` | YES — `test_eval_matchers_sev38.py:105-145` asserts infra failure returns `score=None` and logs WARNING; `test_eval_summary.py:95,127` asserts `guarantee_demonstrated is False` when no clean guard turns | **HOLDS** | `evals/runner.py:236`, `tests/unit/test_eval_matchers_sev38.py:105` |
| **L1-02** `graph_error_to_http` redaction (19 handlers) | PARTIAL — `route_helpers.py:138-158` is redacted (no node_id/node_type leak in the helper itself); 15+ routes call `graph_error_to_http` correctly | **REGRESSED** — Two routes escape the shared helper: `api/routes/locations.py:88` `raise HTTPException(status_code=400, detail=str(exc)) from exc` and `api/routes/economy.py:129` `detail={"code": "CHARACTER_NOT_FOUND", "message": f"Character not found: {exc.node_id}"}`. The `test_route_error_redaction.py` only tests `clock`, `debts`, `groups`, `quest_generation` — not `locations` or `economy`. | **PARTIAL** | `src/npc_engine/api/routes/locations.py:88`, `src/npc_engine/api/routes/economy.py:129` |
| **L1-01** WS per-key connection cap wired | YES — `dialogue_ws.py:77-94 _acquire_ws_slot/_release_ws_slot`; endpoint calls `_acquire_ws_slot` at line 228 and `_release_ws_slot` in finally | YES — `test_sev20_auth_surface.py:106-122` `test_acquire_ws_slot_enforces_cap_and_releases` drives `_acquire_ws_slot` to cap, verifies rejection, verifies slot release; directly exercises the mechanism | **HOLDS** | `src/npc_engine/api/routes/dialogue_ws.py:228`, `tests/unit/test_sev20_auth_surface.py:106` |
| **L1-03** `prompt_builder` debug log env-gate | YES — `dialogue_handler.py:75-87` `resolve_log_prompts()` gates on `settings.LOG_LLM_PROMPTS and settings.ENV == "dev"` | YES — `test_sev19_prompt_redaction.py` tests all four branches (dev+on, staging+on, prod+on, dev+off) with meaningful assertions | **HOLDS** | `src/npc_engine/engines/dialogue/dialogue_handler.py:75`, `tests/unit/test_sev19_prompt_redaction.py` |
| **L1-04** Shipped `.env` `API_KEY_SECRET` staging/prod rejection | YES — `config_validators.py:22-42` `check_api_key_secret()` raises when `_KNOWN_DEV_API_KEY_SECRET` is used outside `dev`; wired in `config.py:257` | YES — `test_sev21_security_hardening.py` verifies `check_api_key_secret` rejects dev key in staging/prod | **HOLDS** | `src/npc_engine/config_validators.py:40`, `tests/unit/test_sev21_security_hardening.py` |
| **L1-06** `IDEMPOTENCY_ENFORCE_HEADER=false` staging/prod gate | YES — `config.py:258-262` raises when `IDEMPOTENCY_ENFORCE_HEADER` is False and `ENV != "dev"` | YES — `test_sev21_security_hardening.py` (contains idempotency validator tests per grep results) | **HOLDS** | `src/npc_engine/config.py:258` |
| **L1-07** `WORLD_ID` default `world_demo` not in `.env` | YES — `config.py:137 WORLD_ID: str = "world"` with comment noting L1-07 fix | YES — `test_sev13_world_state_id.py:10-14` asserts `_WORLD_STATE_ID == "world"` | **HOLDS** | `src/npc_engine/config.py:137` |
| **L3-03** `NPCStateResponse` has raw `dict`/`list[dict]` fields | PARTIAL — `CharacterNode`, `RelationEdge`, `EventNode` sub-models created in `api/response_models/npc_state.py`; `NPCStateResponse` uses them (schemas.py:74-76); however `RelationEdge.relation: dict[str,Any]` and `EventNode.event: dict[str,Any]` are still raw dicts per L3 evidence | PARTIAL — `test_npc_state_response_models.py` asserts `isinstance(resp.character, CharacterNode)` etc., which is meaningful; however the inner `relation` and `event` dict fields remain typed as `dict[str,Any]` | **PARTIAL** | `src/npc_engine/api/response_models/npc_state.py:34,42`, per L3.md evidence |
| **L3-05** `from __future__ import annotations` missing (136→0 via ruff) | **NOT FIXED** — 138 files still missing the import (verified by script: `config.py`, entire `auth/`, `schema/`, `mutation/`, `type_registry/` packages) | No guard test; absence of the ruff fix is visible in the file count | **REGRESSED** | 138 files including `src/npc_engine/config.py`, `src/npc_engine/auth/` package |
| **L3-07** generic upsert typed | NOT FULLY FIXED — `generic_node_service.py:107` still `def upsert_node(self, node_type: str, payload: dict[str,Any]) -> dict[str,Any]:`; L3 evidence confirms HOLDS status (dict throughout) | The L9-02 fix routed the world_state update through `patch_node` directly, bypassing the generic service; the generic service itself remains untyped but L9-02 is no longer coupled to it | **PARTIAL** | `src/npc_engine/graph/generic_node_service.py:107` |
| **L2-02** `observability/` absent from layer model | YES — `DECISIONS.md:704-708` DEC-069 documents `observability/` as non-code asset directory explicitly outside layer rank model | No guard test needed (doc decision); DEC entry exists | **HOLDS** | `project-harness/DECISIONS.md:704` |
| **L2-04** `gossip_handler` misleading "session.run" comments | YES — gossip_handler.py comments now describe "1 batched graph read" / "1 batch write" accurately (lines 113-115, 165, 179-180); all session.run calls removed from handler | No formal guard test; the comment accuracy is verifiable by grep (0 `session.run` in gossip_handler.py) | **HOLDS** | `src/npc_engine/engines/gossip/gossip_handler.py:113` |
| **L5-02** Stray `refactor/` directory | YES — `find . -type d -name refactor` returns nothing; L5 evidence confirms RESOLVED | N/A — directory removed | **HOLDS** | per L5.md evidence |
| **L6-02** Prompt-location doc drift | YES — `CLAUDE.md` updated to `src/npc_engine/prompts/` with eval-judge sub-path (CLAUDE.md:56) | N/A — doc fix | **HOLDS** | `project-harness/CLAUDE.md:56` |
| **L7-03** Backend Literal vs registry divergence | YES — `engines/llm/factory.py:33-42` uses pure registry keyed on `str`; `llm_config_models.py:23` `backend: str = Field(min_length=1)` with no hard Literal; factory comment "no Literal needs editing" | YES — `test_llm_factory.py` tests `test_factory_raises_for_unknown_backend` exercises the runtime path | **HOLDS** | `src/npc_engine/engines/llm/factory.py:42`, `src/npc_engine/engines/llm_config_models.py:23` |
| **L7-07** Adapter self-registration silent import side-effect | YES — `engines/llm/__init__.py:19` explicitly imports factory for registration side-effect; comment names L7-07 | YES — `test_llm_factory.py:49-81` verifies mock and ollama backends return correct adapter types after import | **HOLDS** | `src/npc_engine/engines/llm/__init__.py:19` |
| **L5-04** Unlogged TODO | No `# TODO` strings found in `src/npc_engine/` by grep | N/A | **HOLDS** | (grep clean) |

---

## 2. Counts

| Verdict | Count | IDs |
|---------|-------|-----|
| HOLDS | 16 | L9-01, L9-02, L4-01, L4-04, L4-07, L1-01, L1-03, L1-04, L1-06, L1-07, L2-02, L2-04, L5-02, L6-02, L7-03, L7-07 |
| PARTIAL | 7 | L9-05, L4-02, L4-03, L1-02, L3-03, L3-07, L5-04* |
| REGRESSED | 2 | L4-06, L3-05 |

*L5-04 is HOLDS — listed as partial in table description but clean on grep.

---

## 3. Findings

### [L8-01] L4-02: seed-log guard test is asserted-not-measured (persists)
**Severity: MEDIUM · Confidence: Confirmed**

`tests/unit/test_sev22_rng_determinism.py:75-83` is named `test_secret_propagation_logs_seed` and takes a `caplog: pytest.LogCaptureFixture` parameter but never calls `caplog.at_level()`, never enters a `with caplog.at_level(logging.DEBUG)` block, and never asserts on `caplog.records`. The function body only calls `_secret_rng_seed(...)` and asserts the return type. The production log emission at `gossip_handler.py:391` goes untested — a future refactor deleting the `LOGGER.debug(...)` call would leave this test green. This is the exact asserted-not-measured pattern the prior review flagged as L4-02 "Fix now."

**Fix sketch:** Add `with caplog.at_level(logging.DEBUG, logger="npc_engine.engines.gossip.gossip_handler"):` and assert `any("gossip_secret_rng" in r.message for r in caplog.records)`.

---

### [L8-02] L1-02: Two routes escape the `graph_error_to_http` guard (new residuals)
**Severity: HIGH · Confidence: Confirmed**

`src/npc_engine/api/routes/locations.py:88`:
```python
raise HTTPException(status_code=400, detail=str(exc)) from exc
```
`src/npc_engine/api/routes/economy.py:129`:
```python
detail={"code": "CHARACTER_NOT_FOUND", "message": f"Character not found: {exc.node_id}"},
```

The `locations.py` site leaks the full `ValueError` message (which may contain internal path/schema info). The `economy.py` site leaks `exc.node_id` — an internal graph node identifier — in the HTTP response body. Both bypass `graph_error_to_http`. The existing guard test `test_route_error_redaction.py` does not cover either route. The L1 evidence file predates these routes.

**Fix sketch:** Replace `detail=str(exc)` in `locations.py:88` with a static `"Invalid location hierarchy"`. Replace `f"Character not found: {exc.node_id}"` in `economy.py:129` with `"Character not found"`. Add test cases for both routes to `test_route_error_redaction.py`.

---

### [L8-03] L3-05 + L4-06: Two "Fix now" items not landed
**Severity: MEDIUM · Confidence: Confirmed**

**L3-05** (`from __future__ import annotations` ruff one-liner): 138 of 469 Python files (29%) still lack the import, including the entire `auth/` (6/6 files), `schema/` (11/11), `mutation/` (3/3), `type_registry/` (12/13) packages, and `config.py`. The prior review noted this was a one-liner ruff fix. It was not applied.

**L4-06** (eval test manual `sys.path`): five eval test files (`test_eval_matchers.py`, `test_eval_matchers_sev38.py`, `test_eval_runner_guards.py`, `test_eval_summary.py`, `test_anti_hallucination_runner.py`) still use `sys.path.insert(0, ...)` to import the evals package. The prior review marked this "Fix now" with the suggested fix of making `evals/` a proper `pyproject.toml`-declared package or using `conftest.py` path injection. Both remain unfixed.

**Fix sketch (L3-05):** `ruff check --select=FA102 --fix src/` (adds missing `from __future__ import annotations`). **Fix sketch (L4-06):** Add `evals` to `pyproject.toml` `[tool.pytest.ini_options] pythonpath` or install it as an editable package alongside `src/`; remove all `sys.path.insert` calls from eval test files.

---

### [L8-04] L9-05: BUILD_SHA guard test asserted-not-measured (structural gap)
**Severity: LOW · Confidence: Confirmed**

`test_boot_smoke.py` only asserts `/health` returns status 200. No test asserts that the `version` field in the response body is non-"dev" in any environment, or that the Dockerfile `ARG BUILD_SHA` injects a real value. The CI pipeline has no smoke run that checks the Docker image's `/health` version field. The feature exists (`system.py:47`), but the "stale image" guard the prior review asked for (L9-05: "build-SHA in /health") is untested end-to-end.

**Fix sketch:** Add a test asserting `response.json()["data"]["version"]` equals the value passed via `BUILD_SHA` env override in the `_MINIMAL_SETTINGS` fixture.
