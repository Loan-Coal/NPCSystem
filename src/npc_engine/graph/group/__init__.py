"""
Package: graph.group
Layer: graph
Purpose: Group membership reads and writes.
Public surface: submodules — group_queries,group_service.
Does NOT: orchestrate engine workflows or call LLMs.
Dependencies injected: None (graph data-access submodule package).
"""

from __future__ import annotations
