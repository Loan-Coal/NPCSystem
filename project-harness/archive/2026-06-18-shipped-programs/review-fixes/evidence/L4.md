# L4 Test Efficacy Review Evidence

Branch: munich-demo, HEAD 327180a
Date: 2026-06-13
Scope: Test quality, coverage gaps, asserted-not-measured guards, mock LSP validity.
New code since prior review: F1.6 (scheme advance + detection ticks),
F2.3 (/npc/{id}/schemes route), G2.2 (intrigue board panel + poller).

---

## 1. Prior SEV Re-Verification Table

Ran: make test (2141 passed, 0 failed, 29 skipped) and
python -m pytest tests/ -q --cov=npc_engine --cov=matchers --cov=summary --cov-fail-under=80.
Coverage gate: 86.64%.

| Prior ID | Title | Status | Evidence |
|----------|-------|--------|----------|
| L4-01 | 8 unit failures blocking make check | FIXED | 2141 passed, 0 failed. ISSUES.md entry [FIXED]. |
| L4-02 | Seed-log test asserted-not-measured (SEV-22) | PARTIAL | No test_sev22_seed_log.py. Addressed via test_gossip_distortion_sev36.py::TestGossipHandlerLogsDistortionProbability which monkeypatches LOGGER.debug and asserts seed and distortion_probability appear in logs. Exercises _build_write_params directly, not run_tick end-to-end. A regression in the run_tick call chain would be invisible. |
| L4-03 | evals/runner.py excluded from coverage gate | HOLDS | test-cov target measures --cov=npc_engine --cov=matchers --cov=summary only; runner absent. Manual check: evals/runner.py is 24% (164 stmts, 125 missed). Gate passes only because runner is excluded. |
| L4-04 | SEV-18 gossip mock missing distortion_level | FIXED | test_gossip_distortion_sev36.py has 10 passing tests. compute_distortion_probability, compute_confidence, and gossip_distort all exercised directly. |
| L4-05 | Guard cases lack positive keyword_any | FIXED | test_eval_matchers.py::test_keyword_any_matches and ::test_keyword_any_no_match_fails both pass. SEV-38 added EvalConfigError for fewer than 2 keywords. |
| L4-06 | Eval tests use manual sys.path injection | HOLDS | 5 files still inject: test_anti_hallucination_runner.py:17, test_eval_matchers.py:22-23, test_eval_matchers_sev38.py:23-24, test_eval_runner_guards.py:15-16, test_eval_summary.py:15-16. Pattern unchanged; tests pass. |
| L4-07 | tone_judge infra failure keeps guarantee_demonstrated=True | FIXED | SEV-38 introduced JudgeResult(score=None, error=infra_failure). test_tone_judge_timeout_fails_closed and test_tone_judge_unreachable_fails_closed confirm score=None. runner.py:395 gates on guarantee_demonstrated before reporting. |

---

## 2. New Findings

### [L4-08] mark_scheme_discovered has no unit test
Severity: MEDIUM . Confidence: HIGH

src/npc_engine/graph/scheme_writer.py:149-152 (result-consumption path of mark_scheme_discovered)
is the uncovered portion causing scheme_writer.py to sit at 81%. tests/unit/test_scheme_writer.py
tests upsert_scheme and add_scheme_step only -- mark_scheme_discovered is absent. The only coverage
comes from the integration test which skips when Neo4j credentials are absent. The branch
(return record is not None -- active->discovered vs. no-op) has zero isolated unit coverage.

File: src/npc_engine/graph/scheme_writer.py:149-152;
      tests/unit/test_scheme_writer.py (function absent)

Fix: add test_mark_scheme_discovered_returns_true_when_record_found and
test_mark_scheme_discovered_returns_false_when_no_record using AsyncMock session with
result.single() returning a mock row or None.

---

### [L4-09] investigation_service.py is 0% covered -- graph CREATE writes with no tests
Severity: HIGH . Confidence: HIGH

src/npc_engine/graph/investigation_service.py:12-246 has 28 statements, 0 covered.
Six public async functions using raw Cypher CREATE (not MERGE): create_evidence, implicate,
set_evidence_location, create_deduction, update_deduction_status, record_suspect. Raw CREATE
means duplicate calls silently create duplicate nodes with uuid4() IDs. CLAUDE.md rule: every
public function needs happy-path and failure test. None exist.

File: src/npc_engine/graph/investigation_service.py:12-246

Fix: create tests/unit/test_investigation_service.py with AsyncMock session for all 6 functions.
Add integration test for duplicate-node guard.

---

### [L4-10] location_graph route is 0% covered -- 422 guards untested
Severity: MEDIUM . Confidence: HIGH

src/npc_engine/api/routes/location_graph.py:11-145 has 36 statements, 0 covered. Underlying
query functions in graph/location_graph_queries.py have 16 unit tests, but the HTTP layer is
never exercised. The route-level 422 guards (kind not in _VALID_KINDS and from_id == to_id)
are unreachable by any test -- asserted-not-measured for the validation path.

File: src/npc_engine/api/routes/location_graph.py:11-145

Fix: add tests/unit/test_location_graph_route.py with FastAPI TestClient and monkeypatched
query functions, covering the 422 guards and 404 no-path response.

---

### [L4-11] schedule_queries.py is 24% covered -- scheduler reads almost entirely unmocked
Severity: MEDIUM . Confidence: HIGH

src/npc_engine/graph/schedule_queries.py has 49 stmts, 37 missed (24%). Contains NPC schedule
lookup and time-window query functions used by the routine engine. No unit or integration tests
exist. The AsyncMock pattern for Neo4j reads is established in test_scheme_reader.py.

File: src/npc_engine/graph/schedule_queries.py:63-163

Fix: add tests/unit/test_schedule_queries.py using the AsyncIter/row mock pattern.

---

### [L4-12] Intrigue board panel (G2.2) tests are crash-safety only -- no behavioral assertions
Severity: LOW . Confidence: HIGH

demo_game/tests/test_scheme_board_panel.py::TestDraw has two tests: draw_no_schemes_no_crash
and draw_with_schemes_no_crash. Neither asserts that discovered vs. undiscovered schemes render
differently. A regression where discovered=True and discovered=False schemes render identically
would be invisible. test_npc_schemes_poller.py is behaviorally strong (6 tests). The panel
tests add no behavioral signal beyond no-crash.

File: demo_game/tests/test_scheme_board_panel.py:59-69

Fix: add a test passing a mixed discovered/undiscovered scheme list and asserting font.render
is called with the discovered scheme goal text.

---

### [L4-13] evals/runner.py core execution path 24% covered and excluded from gate (L4-03 HOLDS)
Severity: MEDIUM . Confidence: HIGH

Gate measures --cov=npc_engine --cov=matchers --cov=summary; runner is absent from measurement.
When measured manually: 24% (125/164 stmts missed). guard-helper tests cover _is_guard_case,
_expected_with_guards, _FALLBACK_LINES. The primary execution path -- run_cases(), HTTP call
loop, per-case result assembly, guarantee_demonstrated gate at runner.py:395, write_report --
is entirely untested.

File: evals/runner.py:119-404

Fix: add tests/unit/test_eval_runner_run_cases.py using monkeypatched httpx.Client to simulate
a full run_cases() pass with one guard case; verify summary has guarantee_demonstrated=True.

---

### [L4-14] asyncio.iscoroutinefunction in sev05 store tests emits deprecation warnings
Severity: LOW . Confidence: HIGH

tests/unit/test_sev05_store_locking.py:33,38,96,101 use asyncio.iscoroutinefunction(), which
Python 3.14 deprecated. Confirmed: 4 DeprecationWarnings per run in test output. The concurrent
tests provide actual behavioral coverage; these 4 structural checks are noise.

File: tests/unit/test_sev05_store_locking.py:33,38,96,101

Fix: replace with inspect.iscoroutinefunction (3.14+ recommendation).

---

## 3. Run Results Summary

| Command | Result |
|---------|--------|
| make test | 2141 passed, 0 failed, 29 skipped |
| make test-demo | 1081 passed, 0 failed |
| Coverage gate (make test-cov) | 86.64% PASS (>=80%) |
| evals/runner.py (measured manually) | 24% -- excluded from gate |
