# L7 — OCP / Expansion Review

**Branch:** munich-demo · **HEAD:** 327180a · **Agent:** architect (read-only; evidence transcribed by orchestrator — agent lacks Write).

## Prior-SEV re-verification

| SEV | Title | Verdict | Evidence |
|-----|-------|---------|----------|
| L7-01 | Distortion type closed 3-place if-chain | **FIXED (residual)** | `STRATEGY_REGISTRY` in `distortion_strategy.py:47-54`; dispatch reads registry at `gossip_distort.py:231`. Residual: `DistortionType` Literal (`gossip_distort.py:34`) + frozen `REGISTRY_KEYS` tuple (`distortion_strategy.py:54`) still closed → see L7-01-R. |
| L7-02 | `location_writer.py` absent | **FIXED** | `graph/location_writer.py` exists with `write_part_of()` (idempotent MERGE) + `delete_part_of()`. |
| L7-03 | Backend Literal vs registry divergence | **FIXED** | `llm_config_models.py:33-47` validates `backend` against `registered_backends()` (live dict), not a Literal. |
| L7-06 | No `EmotionModelProtocol`; VAD hardcoded | **FIXED (residual)** | `emotion_model_protocol.py` defines the Protocol; `VadEmotionModel` + `TraitModulatedEmotionModel` implement it; `emotion_model_factory.build_emotion_model()` selects. Residual: 2-branch if + `EMOTION_MODEL` Literal (`config.py:148`) → L7-02-R. |
| L7-07 | Adapter self-registration silent import side-effect | **PARTIAL** | `engines/llm/__init__.py:19` documents the side-effect import but mechanism unchanged; a 3rd-party adapter needs `__init__.py` edited → L7-05. |
| L7-08 | Fat `LLMClientProtocol` (ISP) | **HOLDS** | `protocols.py` still bundles `generate`/`generate_structured`/`stream`/`health_check`/`model_name`. Streaming-only or structured-only adapters can't satisfy it. |

## Findings

### [L7-01-R] `DistortionType` Literal + frozen `REGISTRY_KEYS` are closed despite open STRATEGY_REGISTRY
Severity: MEDIUM · Confidence: Confirmed
`gossip_distort.py:34` (`DistortionType = Literal[...]`), `:41` (`distortion_type: DistortionType | None`), `distortion_strategy.py:54` (`REGISTRY_KEYS = tuple(STRATEGY_REGISTRY)` frozen at import). A 5th strategy is unreachable at runtime and rejected by the closed Literal; registry and model live in different files with no coupling prompt.
Fix: `DistortionType = str` (or validator against `STRATEGY_REGISTRY`); make `REGISTRY_KEYS` reflect live state.

### [L7-02-R] `emotion_model_factory` is a 2-branch closed if; `EMOTION_MODEL` Literal must grow
Severity: LOW · Confidence: Confirmed
`emotion_model_factory.py:51-53` + `config.py:148`. A 3rd model needs both the factory and the Literal edited. LLM factory (L7-03) shows the correct registry pattern.
Fix: `_EMOTION_MODEL_REGISTRY` dict + settings validator; `EMOTION_MODEL: str`.

### [L7-03-R] TTS backend selection is a closed if/elif in `dependencies.py`; no TTS factory registry
Severity: LOW · Confidence: Confirmed
`dependencies.py:141-146` + `config.py:140`. A 3rd TTS backend requires editing the composition root (human-approval gated) + the Literal. `TTSClientProtocol` exists but construction is an if-chain.
Fix: `engines/tts/tts_factory.py` mirroring the LLM factory; `TTS_BACKEND: str`.

### [L7-04] `_LABEL_TO_VALENCE_AROUSAL` hardcoded closed dict of 5 mood labels across two modules
Severity: LOW · Confidence: Confirmed
`mood_contagion_engine.py:34-40` + `emotion_state.derive_label()` (5 hardcoded branches). A 6th label silently maps to `(0,20)` neutral default. OCP violation spanning two modules with no coupling enforcement.
Fix: extract canonical label→(valence,arousal) table to `emotion_state.py`; import it in the contagion engine.

### [L7-05] `engines/llm/__init__.py` must be edited to register every new 3rd-party backend
Severity: LOW · Confidence: Confirmed
`engines/llm/__init__.py:19` — adding `anthropic_adapter.py` requires editing `__init__.py`. OCP strict says new backends add a new file only.
Fix: plugin discovery (entry points / package-dir glob), or formally document `__init__.py` as the one allowed edit point.

### [L7-06] Scheme step kind is an untyped free string — no `StepKind` enum/registry
Severity: LOW · Confidence: Confirmed
`covert_event_factory.py:23` (`COVERT_SCHEME_EVENT_TYPE: str = "scheme_advance"`); `SchemeStepView` (`scheme_reader.py:104`) has no step-kind discriminator. New covert step kinds require replicating the factory pattern with no seam.
Fix: `SchemeStepKind` enum/Literal + a `step_kind` param on `build_covert_event_props`.

### [L7-07] `CliqueFormationEngine._AFFECTION_THRESHOLD = 70` hardcoded magic number
Severity: LOW · Confidence: Confirmed
`clique_formation_engine.py:34` (also `_INITIAL_COHESION = 10`, `_STALE_CLIQUE_AGE_TICKS = 50`). CLAUDE.md strict: no raw numeric thresholds. Settings injected for tick interval but not these.
Fix: add `CLIQUE_*` keys to `config.py`; read from settings in the constructor.

### [L7-08] Fat `LLMClientProtocol` bundles streaming + structured (ISP, holds)
Severity: LOW · Confidence: Confirmed
`protocols.py:77`. A streaming-only or text-only backend must stub `generate_structured`/`stream`, risking LSP drift.
Fix: split into `LLMGenerateProtocol` / `LLMStructuredProtocol` / `LLMStreamProtocol`.

## Counts
MEDIUM 1 (L7-01-R) · LOW 7 (L7-02-R, L7-03-R, L7-04, L7-05, L7-06, L7-07, L7-08)

## Needs human decision (roadmap pre-work)
- **DistortionType Literal vs registry** — `str` (loses enum checking) vs extensible enum; affects `mutation_type` serialization on BELIEVES_RUMOR edges.
- **LLMClientProtocol ISP split** — decide split boundary before the SDK OpenAPI contract freeze (breaking change after clients build against it).
- **TTS factory registry vs `dependencies.py` edit** — only needed if "bring your own TTS" is in the commercial pitch.
